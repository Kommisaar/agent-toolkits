---
id: frontend-web
label: 前端 Web
applies-to: 浏览器界面、SPA、SSR、组件库和 Web E2E
---

# 前端 Web

## 识别

出现浏览器入口、组件目录、路由、样式资源，或 React、Vue、Svelte、Angular、Next.js、
Nuxt、Vite 等配置时应用本包。项目同时使用 TypeScript/JavaScript 时组合读取
`typescript` 包，本包不重复定义包管理器。

## 验证维度

- 静态检查、类型检查和生产构建；
- 组件、状态管理和路由测试；
- SSR、hydration、客户端导航和错误边界；
- 浏览器 E2E 与关键用户路径；
- 键盘操作、焦点顺序、语义结构和可访问名称；
- 不同视口、主题、语言和长文本；
- 控制台错误、失败请求和资源加载；
- 必要时的视觉截图或录屏证据。

以仓库已有脚本和验收要求决定实际维度。没有配置某类测试时标记 `UNVERIFIED`，不要为了
本轮任务临时引入整套框架。

## 本地服务

1. 只使用任务包指定的启动命令、端口和环境文件。
2. 不连接生产后端，不读取真实账号或密钥。
3. 启动前检查端口占用；不要终止无法证明由本轮创建的进程。
4. 等待明确的健康信号后再运行浏览器验证，避免固定长时间 sleep。
5. 记录自己启动的 PID，并在验证结束后停止；非本轮进程保持不动。

## 浏览器验证

- 工具可用时先获取页面结构，再执行交互和视觉确认。
- 优先覆盖本次改动影响的最短关键路径，再运行项目已有完整 E2E。
- 每个失败保留 URL、步骤、期望、实际结果、控制台或网络证据。
- iframe、验证码、登录、支付和人工确认造成阻塞时如实报告，不绕过安全机制。
- 截图用于证明结果，不用截图代替行为断言。
- 不自动接受、重录或批量更新视觉快照。

## 门禁建议

按项目契约从快到慢执行：

1. 格式、lint、类型检查；
2. 受影响组件和状态逻辑测试；
3. 生产构建；
4. 关键路径 E2E；
5. 项目要求的完整浏览器或视觉套件。

Verifier 运行浏览器测试时必须独占对应 worktree 和本地服务。浏览器工具不可用不代表
测试通过，应在结果中说明替代证据和残余风险。

## 测试资源预算

多 worktree 并行开发时，完整测试套件是最主要的可复用 CPU 消耗。识别 Vitest 后按以下
顺序评估（实测先例：全量墙钟 142s → 53s、累计 CPU -90%，断言本身仅占约 4%）：

1. 先测量再动手：用一次全量跑的分项数据（transform / collect / environment /
   tests）定位大头；组件库依赖树在隔离模式下的重复执行（collect）和 DOM 环境重建
   （environment）通常远大于断言本身。
2. `deps.optimizer.web` 预打包重依赖（组件库、`react-dom`、测试库），缓存位于
   `node_modules/.vitest`——重装依赖即抹除缓存，首跑需重建。
3. `projects` 分组隔离：依赖干净 `document` 的断言（如遍历
   `document.styleSheets` / `cssRules` 的样式扫描）单独 `isolate: true` 组，其余
   `isolate: false` 共享 worker 环境与模块图；新增样式扫描断言必须登记进隔离组。
   project 不继承顶层 `test` 的 `environment` 与 `deps`，必须逐 project 显式声明，
   漏配会静默回退（node 环境 / 无预打包），collect 可暴涨数倍。
4. 纯逻辑测试用文件头 `// @vitest-environment node` 跳过 DOM 环境创建。
5. `maxWorkers` 按多 worktree 并行预算定值：并行实例数 × workers ≤ 物理核数
   （例：22 核、3 个并行门禁 → 6 workers）；单跑求极速用 CLI 参数覆盖，不改配置。
6. 更换环境实现（如 happy-dom 替换 jsdom）会改变可见行为前提（`matchMedia` 缺省、
   `computedStyle` 归一化、对话框交互差异）；收益不足时弃用并留档，不改断言迁就。
7. 并行负载下的偶发失败先隔离复跑确认再定性，不得用复跑成功掩盖首次失败。

## 生成物与污染

重点检查测试截图、视频、trace、coverage、快照、构建目录、框架缓存和生成路由。只有
任务包明确允许的报告目录可以保留为未跟踪输出；已跟踪快照或生成文件变化一律按源码
变更处理。

## 常见风险

- 只验证首次加载，遗漏客户端导航；
- 开发构建正常但生产构建或 SSR 失败；
- loading、empty、error 和权限状态缺失；
- 点击可用但键盘、焦点或屏幕阅读器不可用；
- 测试依赖固定时区、动画、随机数据或外部网络而不稳定；
- mock 与真实 API 状态码、字段可选性或默认值不一致。
